name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24.5'
        
    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-
          
    - name: Download dependencies
      run: go mod download
      
    - name: Build for multiple platforms
      run: |
        # Create release directory
        mkdir -p release

        # Build for different platforms
        GOOS=linux GOARCH=amd64 go build -ldflags="-s -w" -o release/mon-linux-amd64 ./cmd/mon
        GOOS=linux GOARCH=arm64 go build -ldflags="-s -w" -o release/mon-linux-arm64 ./cmd/mon
        GOOS=darwin GOARCH=amd64 go build -ldflags="-s -w" -o release/mon-darwin-amd64 ./cmd/mon
        GOOS=darwin GOARCH=arm64 go build -ldflags="-s -w" -o release/mon-darwin-arm64 ./cmd/mon
        GOOS=windows GOARCH=amd64 go build -ldflags="-s -w" -o release/mon-windows-amd64.exe ./cmd/mon
        GOOS=windows GOARCH=arm64 go build -ldflags="-s -w" -o release/mon-windows-arm64.exe ./cmd/mon
        
    - name: Create checksums
      run: |
        cd release
        sha256sum * > checksums.txt
        
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: monday-cli-binaries
        path: release/
        
    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/')
      uses: softprops/action-gh-release@v1
      with:
        files: release/*
        generate_release_notes: true
      env:
        GITHUB_TOKEN: ${{ secrets.ACTION_TOKEN }}
